# chars

We were presented with a chars.zip file and upon extracting we got a linux binary called chars, but upon executing it only tells us that we wont get the flag from it and suggests us to use ``strings``.

Upon running ``strings ./chars`` and searching for the flag format, we find the flag